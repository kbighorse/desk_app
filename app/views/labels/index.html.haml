%h1 Labels

%table{ class: "table table-striped labels" }
  %thead
    %th
      Name
  %tbody
    - @labels.each do |label|
      %tr
        %td
          = label.name
%p
  = link_to "New Label", new_label_path
